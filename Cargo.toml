[package]
name = "ezhomie"
version = "0.1.0"
edition = "2021"
description = "A macOS-first CLI for starting and ending the workday."
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

